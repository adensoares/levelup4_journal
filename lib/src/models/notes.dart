import 'package:flutter/cupertino.dart';

class Note {
  String title;
  String description;
  String date;
  Color color;

  Note({
    required this.title,
    required this.description,
    required this.date,
    required this.color,
  });
}
